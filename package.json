{
  "name": "antigravity-context-monitor",
  "displayName": "Antigravity Context Window Monitor",
  "description": "Real-time context window usage monitor for Antigravity (实时显示 Antigravity 当前会话的上下文窗口使用量和剩余量)",
  "version": "1.11.1",
  "publisher": "AGI-is-going-to-arrive",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AGI-is-going-to-arrive/Antigravity-Context-Window-Monitor.git"
  },
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/fork-entry.js",
  "contributes": {
    "commands": [
      {
        "command": "antigravity-context-monitor.showDetails",
        "title": "Show Context Window Details / 显示上下文窗口详情"
      },
      {
        "command": "antigravity-context-monitor.refresh",
        "title": "Refresh Context Window Monitor / 刷新上下文窗口监控"
      },
      {
        "command": "antigravity-context-monitor.switchLanguage",
        "title": "Switch Display Language / 切换显示语言"
      },
      {
        "command": "antigravity-context-monitor.exportConversation",
        "title": "Export Conversation Context / 导出对话上下文"
      },
      {
        "command": "antigravity-context-monitor.toggleDump",
        "title": "Toggle Auto Context Dump / 切换自动上下文落盘"
      },
      {
        "command": "antigravity-context-monitor.openDumpFolder",
        "title": "Open Context Dump Folder / 打开上下文落盘目录"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "antigravity-context-monitor",
          "title": "Context Monitor",
          "icon": "$(pulse)"
        }
      ]
    },
    "views": {
      "antigravity-context-monitor": [
        {
          "type": "webview",
          "id": "antigravity-context-monitor.sidebarView",
          "name": "Context Monitor",
          "icon": "$(pulse)"
        }
      ]
    },
    "configuration": {
      "title": "Antigravity Context Monitor / 上下文监控设置",
      "properties": {
        "antigravityContextMonitor.pollingInterval": {
          "type": "number",
          "default": 5,
          "description": "Polling interval in seconds / 轮询间隔（秒）"
        },
        "antigravityContextMonitor.contextLimits": {
          "type": "object",
          "default": {
            "MODEL_PLACEHOLDER_M37": 1000000,
            "MODEL_PLACEHOLDER_M36": 1000000,
            "MODEL_PLACEHOLDER_M47": 1000000,
            "MODEL_PLACEHOLDER_M18": 1000000,
            "MODEL_PLACEHOLDER_M35": 1000000,
            "MODEL_PLACEHOLDER_M26": 1000000,
            "MODEL_OPENAI_GPT_OSS_120B_MEDIUM": 128000
          },
          "description": "Model context window limits (in tokens). Key is the model's internal ID. Override if needed. IDs: MODEL_PLACEHOLDER_M37=Gemini 3.1 Pro (High), MODEL_PLACEHOLDER_M36=Gemini 3.1 Pro (Low), MODEL_PLACEHOLDER_M47=Gemini 3 Flash, MODEL_PLACEHOLDER_M35=Claude Sonnet 4.6 (1M GA), MODEL_PLACEHOLDER_M26=Claude Opus 4.6 (1M GA), MODEL_OPENAI_GPT_OSS_120B_MEDIUM=GPT-OSS 120B / 各模型上下文窗口大小（token 数），key 为模型内部 ID。如需可自行覆盖。"
        },
        "antigravityContextMonitor.compressionWarningThreshold": {
          "type": "number",
          "default": 200000,
          "minimum": 10000,
          "description": "Estimated compression trigger threshold (in tokens). Status bar turns yellow/red based on this value instead of the model's full context limit. Default 200K matches Antigravity's internal compression point. Set to a higher value (e.g. 900000) if your model supports larger context before compression. / 估算压缩触发阈值（token 数）。状态栏颜色基于此值而非模型上下文上限。默认 200K 匹配 Antigravity 内建压缩线。如果模型支持更大上下文可调高（如 900000）。"
        },
        "antigravityContextMonitor.statusBar.showContext": {
          "type": "boolean",
          "default": true,
          "description": "Show context usage (e.g. 45k/1M, 4.5%) in status bar / 状态栏显示上下文用量"
        },
        "antigravityContextMonitor.statusBar.showQuota": {
          "type": "boolean",
          "default": true,
          "description": "Show current model quota indicator (e.g. 🟢85%) in status bar / 状态栏显示当前模型额度"
        },
        "antigravityContextMonitor.statusBar.showResetCountdown": {
          "type": "boolean",
          "default": true,
          "description": "Show quota reset countdown (e.g. ⏳4h32m) in status bar / 状态栏显示重置倒计时"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npx -y @vscode/vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
